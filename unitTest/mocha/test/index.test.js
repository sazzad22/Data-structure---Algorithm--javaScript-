const assert = require('assert');

describe("Simple Math test", () => {
    it("should return 4", () => {
        assert(2 + 2, 4);
    })
    it("should return 9", () => {
        assert(3 * 3, 9);
    })
} )